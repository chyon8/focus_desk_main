# 랜딩페이지 — 진행 상태 (2026-09-13)

> 새 세션에서 **"논의계속"** 이라고 하면 이 파일부터 읽는다. 아래 "다음에 할 일"을 사용자와 같이 정하고 시작한다.

## 지금 있는 파일

| 파일 | 무엇 |
|---|---|
| [landing-v2.html](landing-v2.html) | **확정 시안 (2026-09-13).** 히어로가 종이 격자. 스크롤하면 방이 화면을 덮고 뒤로 물러난 뒤 방 사진이 바뀐다. 이제 이 파일 기준으로 만든다 |
| [landing.html](landing.html) | v1 — 안 쓴다. **지우지 않고 둔다.** 히어로 Summer Lake 고정 + 사진 크기 104%↔100% 반복을 해본 파일 |
| [hero-b.html](hero-b.html) · [hero-c.html](hero-c.html) | 첫 화면 대안. B가 v2 히어로가 됐다. 지우지 않고 둔다 |
| [landing.css](landing.css) | 위 파일들이 같이 쓴다 |
| `/tmp/port.mjs` | 캔버스 아트보드 변환기. 세션 끝나면 사라진다. `/tmp/mkv2.mjs`(v2 생성기)는 이제 v1과 안 맞아서 안 쓴다 — v2는 직접 고친다 |
| `../landing-shots/` | **앱 스크린샷 시안 원본 23장 (2026-09-14, 2880×1800).** `tools/`에 공간 JSON 생성·캡처 스크립트. 스크립트 안 경로는 그 세션 scratchpad 기준이라 다시 쓸 때 고쳐야 한다 |

캔버스(첫 화면 비교용, 움직임 없음, v2 확정 전 스냅샷): https://claude.ai/code/artifact/b0d1bd76-c9b6-4ee1-a7b3-35da78020d1c

## 앱 스크린샷 시안 (2026-09-13~14) — 스크린샷 얘기는 여기부터

> **2026-09-15 새 계획:** 컨셉 컷을 전부 **Cloud 단색 하나로 고정**해서 찍는다. 찍어서 고르는 것까지만 하고, 랜딩에 넣는 건 나중에 사용자가 한다. 아래 "확정 기획"은 건드리지 않는다. 계획 전체는 [../APP-DESIGN-RENEWAL.md](../APP-DESIGN-RENEWAL.md) 맨 아래 "스크린샷 계획 (Cloud 고정)". 아래 "결정 대기" 1~3은 답이 나왔다 — 1 컨셉 컷은 Cloud 고정, 2 밝은 카드, 3 사진 위젯은 리뉴얼 단계 5에서 꽉 채운 모양으로 바꿨다. 아래 기록은 피드백·찾은 것·찍는 법 참고용으로 둔다.

캔버스(5페이지): https://claude.ai/code/artifact/069ee526-b693-4616-a412-59f5295c6356
원본: `../landing-shots/` (2880×1800 jpg). 피드백 전 첫 묶음은 `../landing-shots/before/`

**요청 (2026-09-13 사용자)**
- 스크린샷만 만들고 사용자가 보고 판단한다. **랜딩에 적용하지 않는다**
- 기준은 비주얼. 처음 보고 "와 예쁘네", 다른 앱과 비교해도 "개쩐다"가 나와야 한다. 시니어 디자이너 수준
- 처음 기획(A–D)과 다른 조합도 만든다. 기획을 먼저 보여주고 컨펌 후 만든다
- 시안 번호는 1부터 매긴다(E부터 시작하는 번호를 싫어했다). 방 넷 밖 2장(12·13)도 넣는다

**피드백 (2026-09-14 사용자) — 다음 논의의 기준**
- "사진들이 구리다, 위젯들이 구리다. 원래 디자인 문제인가"
- 색 마크(강조색)는 일단 뺀다
- 배경 방 사진이 제대로 보이게, 위젯도 제대로 나오게
- masonry·무드보드 느낌. 레퍼런스는 Milanote 보드 캡처 3장(Research · Moodboard · Project Ideas): 흰 카드, 사진이 곧 카드, 색 스와치 카드, 굵은 타이포 포스터, 카드끼리 겹침

**찾은 것**
- 구려 보인 주원인은 사진 위젯이다. 윗단 여백(40px)과 캡션 줄 때문에 사진이 작고 액자처럼 보인다. 데모 창에 CSS만 넣어 사진을 꽉 채우자 가장 나았다(캔버스 1페이지 세 번째 열). 지금 앱에는 없는 모양이다
- 어두운 방에서는 불투명한 어두운 카드가 무겁다. Atmosphere Light(밝은 카드)가 Milanote에 더 가깝다
- 일반 스톡 사진보다 방 색에 맞춘 디자인 소재(패키지, 스와치, 워드마크, 질감)가 낫다. 스와치·워드마크 이미지는 `tools/make_assets.py`가 만든다
- 투두는 폭이 넓으면 글자가 커져 한 줄만 보인다 → 월드 폭 320 안쪽으로 둔다
- 컬럼 안 메모·투두 카드는 작게 찌그러져 보인다(STATUS 알려진 문제) → 컬럼에는 페이지 카드만
- 페이지 카드에 `title`이 없으면 제목 자리에 "en.wikipedia.org"가 나온다
- 웹 페이지: Unsplash·Flickr 검색은 iStock 광고, Pexels는 Cloudflare 확인, Unsplash 프로필은 봇 확인 → 안 쓴다. Are.na 채널(사진 격자까지 스크롤), GitHub, Wikipedia, Google Earth(첫 방문 안내창을 닫고)는 된다
- Rainy Attic은 파티클 비를 끈다(벽지에 비가 그려져 있어서, rooms.ts). 기획의 "Rainy Attic + 비"는 적용하지 않았다
- 이 맥은 한국어라 달력·웹 페이지가 한국어로 나온다 → 데모 앱은 `-AppleLanguages (en-US)`로 띄운다
- 가상 이야기: 프리랜스 디자이너의 클라이언트 공간 — Ondo(카페 리브랜드) · Birch Inn(캐빈 호텔) · Nightbird(앨범) · Field Mag(여행 매거진). 레일의 공간 이름은 9자를 넘으면 잘린다
- 사진은 Unsplash(무료 라이선스). 랜딩에 실제로 쓸 때는 페이지 속 다른 브랜드 이미지(Are.na 패키지 등)를 따로 확인한다

**캔버스 구성**

| 페이지 | 번호 | 무엇 |
|---|---|---|
| 1 무드보드 | 2 · 14 · 15 | Birch Inn(Snowy Railway) · Ondo(Rainy Attic) · Nightbird(Midnight Observatory) 보드를 세 모양으로 — 어두운 카드 / 밝은 카드 / 사진 꽉 채운 미리보기 |
| 2 새 시안 | 1, 3–7 | 1 여백(Attic, 위젯 3개) · 3 전체 보기(Observatory, 배율 46%) · 4 페이지 하나(Summer Lake 밝은 UI, Google Earth) · 5 컬럼(Snowy Railway, Wikipedia 카드) · 6 사진 없음(Editorial) · 7 확대 3컷 |
| 3 처음 기획 | 8–11 | A 카페(Attic) · B 개발자 출시(Observatory) · C 글쓰기(Summer Lake) · D A를 Paper에 |
| 4 방 넷 밖 | 12–13 | late-summer-aquarium · winter-lake-greenhouse(눈). 1번 내용 |
| 5 피드백 전 | 1–6 | 색 마크가 있던 첫 묶음 + 버린 1번 L자 배치 |

**결정 대기 (사용자)**
1. 어떤 구도·방으로 갈지
2. 카드 밝기 — 어두운 카드 / 밝은 카드
3. 사진 위젯을 꽉 채운 모양으로 앱을 바꿀지. 바꾸면 앱 코드 작업이고, 캡션·줌 같은 기존 기능을 어떻게 둘지 먼저 묻는다

**다시 찍는 법** (`../landing-shots/tools`, 명령은 저장소 루트에서)
- 데모 프로필 `~/Library/Application Support/focus-desk-demo`에 공간과 사진이 그대로 있다
- 띄우기: `unset ELECTRON_RUN_AS_NODE; FOCUS_DESK_PROFILE=demo FOCUS_DESK_DEBUG_PORT=9337 npx vite --config design-ref/landing-shots/tools/vite/vite.config.mjs` (포트 3007, 영어). 창이 뜬다고 먼저 말한다
- 공간 넣기 `node push.mjs specs/s14-15-boards.mjs`, 찍기 `node shot.mjs <공간 id> out/x.png --rail id,id,id,id [--skin fullbleed] [--scroll 주소일부:px] [--clicktext 주소일부:글자]` (tools 폴더에서, 2배로 찍힌다)
- 스와치·워드마크 이미지는 저장소에 없다. 보드 공간(2·14·15)을 다시 넣기 전에 `python3 make_assets.py`로 먼저 만든다
- 사진 검색 `node unsplash.mjs <폴더> "<검색어>"`, 캔버스 파일 `python3 make_canvas.py` → /design 스킬의 seed-canvas로 묶는다
- 끝나면 3007·9337의 내 PID만 끈다

## 다음에 할 일

### 사용자가 해야 하는 것
1. `[LAUNCH DATE]` 날짜 정하기
2. 대기자 폼 서비스 고르기(계정 만들기) · 도메인 사기 · 호스팅 계정
3. 영상 녹화 — 줌 섹션(줌아웃→줌인), 컬럼 섹션(카드를 컬럼에 넣고 빼는 3~4초 루프)
4. 로그인이 필요한 화면 — 세션 섹션(같은 사이트 두 계정, 이름·아바타가 달라야 한다)
5. 판단 — 줌 순서를 뒤집을지(AI가 뒤집은 버전을 만들면 보고 고른다)
6. 판단 — 스크린샷 시안 캔버스에서 고르기: 구도·방, 카드 밝기(어두운/밝은), 사진 위젯을 꽉 채운 모양으로 앱을 바꿀지

### AI가 할 수 있는 것
1. 🟡 데모 공간을 만들고 스크린샷 — 1차 완료(2026-09-14, 위 캔버스). 남은 것: 6번 판단대로 다시 찍기. 히어로 스크린샷, 방 4개 띠(위젯 올라간 상태), Paper 테마 한 장면. 방법은 `landing-shots/tools`
2. 위젯 안 회색 막대를 실제 글자로 (개선점 3)
3. ✅ 반응형 — 폰·태블릿·1081~1439 (2026-09-13, 개선점 1)
4. 헤더 CTA가 스크롤을 따라오게 (개선점 5)
5. 밝은 방에서 흰 글자 대비 (개선점 6)
6. 이미지 최적화 · OG 이미지 (개선점 7)
7. 줌 순서 뒤집은 버전 (개선점 4)
8. 폼 서비스가 정해지면 연결, 배포용 폴더로 정리

## 확정 기획 (2026-09-13)

**기준은 비주얼이 1순위다.** 첫 화면에서 바로 설득돼야 한다.

원칙: 한 화면에 초점 하나(가장 크고 대비가 큰 자리에 제품 화면) · 위젯 안은 실제 내용 · 제품 화면은 방 사진 위에서 · 색 마크는 제품 화면 안에서만 쓰고 뜻은 페이지 전체에서 하나(**clay = 클라이언트 일, denim = 내 일**)

| 섹션 | 무엇을 | 방 |
|---|---|---|
| ① 히어로(종이) | 제목 아래 앱 스크린샷 1장 — 크게, 안 기울임, 창 테두리 포함, 화면 아래에서 잘림. 위젯 4개를 크게 꺼내 띄움(±2°): 메모·웹앱은 제목 양옆, 사진·타이머는 스크린샷 윗모서리. 메모·웹앱에 clay 색 마크(앱 리뉴얼로 막대가 없어지고 카드 전체 옅은 채움이 됐다). 꺼낸 위젯은 앱 CSS 값으로 HTML | Rainy Attic |
| ② 줌(전체 화면) | 실제 영상 하나: 줌아웃 → 방 바꾸기 → 줌인. 밝은 방일 때만 스크림을 짙게 | Summer Lake부터 4장 |
| ③ 문제(종이) | 실제 파비콘·제목 40개가 한 줄에 눌린 탭 줄 | — |
| ④ 컬럼 | 넣고 빼는 3~4초 영상. 컬럼 둘 clay / denim | Midnight Observatory |
| ⑤ 세션 | 같은 사이트를 연 두 공간의 스크린샷을 겹쳐 놓음. 아바타·공간 이름·마크 색이 다름 | Snowy Railway |
| ⑥ 방 띠 | 4장 모두 위젯 올라간 스크린샷. 아래에 *Or keep it plain.* + Paper 한 장면 | 4장 |
| ⑦ 문장 · ⑧ CTA | 그대로 | CTA는 Rainy Attic |

모바일은 스크린샷 전체 폭 + 꺼낸 위젯은 메모·타이머만. 헤더 CTA는 스크롤을 따라온다.

## 데모 공간 시안

히어로 구도는 그대로 두고 이야기와 방만 바꿔 나란히 비교한다. **A부터 만든다.**

| 시안 | 이야기 | 방 | 위젯 | 보려는 것 |
|---|---|---|---|---|
| A | 프리랜서 디자이너, 가상 카페 리브랜드 | Rainy Attic | 메모(결정 3줄) · 할 일 · 사진(`cozy-cafe`) · 웹앱(Figma·Notion·Gmail·Slack) · 브라우저(사진 많은 공개 페이지) · 타이머 | 기본안 |
| B | 개발자, 사이드 프로젝트 출시 | Midnight Observatory | 브라우저(PR 페이지·localhost 미리보기) · 메모(출시 체크리스트) · 웹앱(GitHub·Linear·Vercel·ChatGPT) · 타이머 | 개발자 장면이 더 끌리는지 |
| C | 글쓰기·리서치 | Summer Lake | 긴 메모(실제 문단) · 사진(참고 이미지) · 브라우저(자료 페이지) · 할 일 · 타이머 | 밝은 스크린샷도 베이지 위에서 초점이 되는지 |
| D | A와 같은 내용 | Paper | A와 같음 | 사진 방이 실제로 눈길을 더 끄는지 비교 기준 |

## 실제 자산이 들어갈 자리

| 자리 | 종류 | 무엇을 |
|---|---|---|
| 히어로 사진 카드 | 정지 | 위젯이 올라간 공간. 지금은 월페이퍼만 있고 UI가 없다 |
| 히어로 위젯 4개 | 정지 | 노트·할 일·페이지·타이머 실물. 노트 하나는 진짜 글이 보여야 한다 |
| **줌 섹션** | **영상** | 줌아웃→줌인 실제 녹화. 지금은 CSS 흉내 |
| 컬럼 섹션 | **영상** | 카드를 컬럼에 넣고 빼는 3~4초 루프. 정지로는 설명이 안 된다 |
| 세션 섹션 | 정지 | 같은 사이트 두 계정. 계정 이름·아바타가 실제로 달라야 증거가 된다 |
| 방 4개 띠 | 정지 | **위젯이 올라간 상태**로. 월페이퍼만 있으면 배경화면 앱이다 |
| 마지막 CTA | 없음 | 월페이퍼 그대로 |

스크린샷은 AI가 찍을 수 있다 — 공간 JSON을 `userData/spaces/`에 심고
`FOCUS_DESK_PROFILE=demo npm run dev`로 띄운 뒤 `screencapture`. 로그인 필요한 페이지와 영상은 사용자가.

## v2 개선점 (순서대로)

1. ✅ **반응형 (2026-09-13).** 1440px 이상은 손대지 않았다(요소별 위치·스타일 값을 고치기 전과 비교해 차이 0). 1081~1439는 히어로 위젯만 줄여 글자와 안 겹치게, 1080 이하는 글 아래에 위젯을 겹쳐 놓음(태블릿 메모·사진·타이머, 폰 메모·타이머), 줌 캔버스는 화면 폭에 맞춤, 데모 위젯 위치는 %로. 규칙은 landing-v2.html `/* ── 반응형 ── */` 아래
2. **첫 화면에 제품이 없다.** 종이 위 추상 막대기로 시작한다. 히어로 사진 카드를 진짜 스크린샷으로 바꾸면 구도를 안 바꾸고도 해결된다
3. **위젯 안에 글자가 하나도 없다.** 전부 회색 막대라 무슨 도구인지 안 읽힌다
4. **줌 순서가 거꾸로일 수 있다.** 지금은 줌인으로 들어와 물러난다. 파는 건 "돌아오면 컨텍스트가 그대로"이므로 줌아웃으로 들어와 들어가는 게 맞을 수 있다 — 뒤집어보고 판단
5. **중간에서 살 수가 없다.** 헤더 CTA가 스크롤을 안 따라온다
6. **밝은 방에서 흰 글자가 위험하다.** 지금 `rgba(12,9,7,.40)` 한 겹으로 버틴다. 밝은 방일 때만 더 깔려야 안전
7. **성능.** 월페이퍼는 2026-09-13에 2880px webp(장당 0.1~0.5MB)로 바꿨다. 남은 것: 화면 크기별 이미지(srcset) · OG 이미지
8. **줌 섹션 모서리의 작은 방이 배경과 같은 방일 때가 있다.** 배경이 Snowy Railway·Summer Lake로 바뀌면 모서리 사진과 겹친다

**줌 섹션의 방 순환은 사용자가 원한 동작이다(2026-09-13). 빼자고 하지 않는다.** "사진 여러 개에 줌아웃"은 줌 섹션 하나에서 방 사진이 바뀌는 것이다. 줌 섹션을 여러 개 만드는 게 아니다 — 한 번 틀렸다.

## 정해진 것 (다시 논의하지 말 것)

- **시안은 v2 (2026-09-13).** 나머지 시안 파일은 지우지 않는다
- **레퍼런스**: mindweaver.space(따뜻한 종이 · 기울어 흩어진 물체 · 한정된 팔레트 — 단 마스코트·손그림 화살표·만화 일러스트는 안 가져온다) · techbukket.com/bako-dictionary. **"이것보다 더 세련되게"**
- **스크롤=줌은 안 한다.** 줌은 앱이 위젯을 다루는 방식이지 사이트 조작법이 아니다. 페이지는 그냥 스크롤한다
- **폰트**: IBM Plex Sans KR (앱 UI 폰트). 2026-09-15 앱 디자인 리뉴얼에서 Instrument Sans에서 바뀌었다 — 랜딩 적용 때 랜딩 CSS도 맞춘다
- **유채색은 앰버 하나.** 위젯은 앱 표면 규칙 그대로 — 14px 라디우스, 테두리 없음, 흰 면(`#ffffff`) + 그림자 한 벌(값은 [DESIGN.md](../DESIGN.md) 4장). 2026-09-15 앱 리뉴얼 반영, 옛 값은 16px · 이중 테두리. 랜딩의 앰버 `#e9a23b`는 앱 강조색(밝음 `#a9581b` / 어두움 `#e9a75f`)과 다르다 — 랜딩 적용 때 맞출지 본다
- **방 넷**: Summer Lake(여름·낮·바깥) · Midnight Observatory(밤·바깥) · Rainy Attic(비·밤·실내) · Snowy Railway(겨울·밤·바깥). 원래 계절·시간·안팎을 다 다르게 골랐는데, 2026-09-13 Autumn을 Observatory로 바꾸면서 낮 1 / 밤 3, 실내 1이 됐다 — 알고 감수함
- **뺀 것**: Ghibli Night Tram(사용자가 싫어함) · Afternoon Records(사용자가 뺌) · Autumn Breakfast Nook(사용자가 뺌, 2026-09-13)
- **Swiss Editorial 테마는 방 띠에 안 넣는다.** 방이 아니라 테마(사진 없음, 각진 위젯)라 위젯 표면 규칙·폰트 규칙과 부딪히고 버린 시안과 비슷해 보인다. 보여준다면 따로 한 장면(스크린샷 + *Or keep it plain.*) — 스크린샷 찍을 때 같이
- **섹션마다 실제 제품 화면 1장.** milanote.com에서 가져온 건 이 규칙 하나다. 흰 바탕·로고 소셜프루프·용도별 카드 그리드는 안 가져온다
- **밀라노트와 차이로 팔 것**: 웹이 링크 카드가 아니라 공간 안에서 도는 탭이고, 스페이스마다 로그인이 따로다. 밀라노트는 구조상 못 한다. 반대로 팀 공유·모바일·템플릿은 건드리지 않는다
- **제목 뒤 검은 글로우 금지.** `text-shadow` 번짐도, 가운데만 어둡게 하는 원형 스크림도 안 된다. 사진 전체에 고르게 한 겹. (줌 섹션 캡션 뒤에 원형 스크림이 아직 남아 있다 — 이 규칙과 부딪힌다)
- **카피는 영어**, CTA는 대기자 이메일(서명·공증 전이라). `[LAUNCH DATE]`는 사용자가 채운다
- **히어로 카피 (2026-09-12 확정, 2026-09-13 반영)**
  - H1: *Your new desktop.*
  - Sub: *Every project gets its own space — tabs, notes and tools, exactly where you left them.*
  - 헤드라인이 "이건 뭐다", 서브가 차별점. 이전 버전은 둘 다 "공간" 얘기라 겹쳤다
  - *Your ideas deserve their own spaces.* 는 **방 4개 띠 위 제목**으로 내렸다. 거기선 사진 넷이 근거가 된다
  - *Where your thoughts live* 는 노트앱 문구라 히어로에 안 쓴다. 쓴다면 마지막 CTA 섹션
  - 전제: `Your new desktop.`은 실제 스크린샷이 들어가야 산다(개선점 2번)

## 버린 시안 (되살리지 말 것)

첫 시도는 흰 Swiss 바탕에 회색 테두리 상자를 격자로 깐 것이었다 — 점선 플레이스홀더가 절반을 차지했고, 스크롤을 줌에 묶었다. 사용자가 "2000년대 디자인"이라고 했다. 지금 방향(방 사진 + 실제 앱 표면 + 장면 구성)이 그걸 대체한 것이다.
