# STATUS — 세션 스타팅 포인트

> 매 세션 이 파일부터 읽는다. 다른 문서는 필요할 때만 연다.
> 작업이 끝날 때마다 이 파일을 갱신한다 (최근 완료 / 다음 할 일).

## 현재 단계
**Phase 5 완료. 웹뷰 클리핑 버그 수정됨(D-025). 다음은 배포 준비.**

> 다음 세션 시작 시: 사용자가 "계속해"라고 하면 **배포 준비**부터 시작한다.

## 문서 맵 (필요할 때만 참조)
| 문서 | 내용 | 언제 읽나 |
|---|---|---|
| [VISION.md](VISION.md) | 컨셉, 타겟, 하지 않을 것, 디자인 방향 | 기능/방향 판단이 필요할 때 |
| [DECISIONS.md](DECISIONS.md) | 스택·기술 결정 + 이유 (append-only) | 기술 선택하거나 뒤집고 싶을 때 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 폴더 구조, 좌표계, 상태, 브라우저 전략 | 코드 작성 전 |
| [ROADMAP.md](ROADMAP.md) | Phase별 목표 + 검증 기준 | 다음 작업 정할 때 |

## 최근 완료
- 2026-08-16: MVP 코드 검토, 쓰레기 정리, 계획 문서 5종 작성
- 2026-08-16: **Phase 0** — MVP 소스 `legacy/`로 이동, `src/` 새 골격, Tailwind v4 정식 설치(CDN 제거), electron main CJS 빌드 수정(D-009), IPC 분리, vitest·electron-builder 도입
- 2026-08-16: **Phase 1** — 카메라 팬/줌(zoom-to-cursor), 월드 좌표 위젯 드래그/리사이즈, WebContentsView 카메라 동기화 PoC 성공(D-010). 검증: bounds·zoom 실측값 일치
- 2026-08-16: **Phase 2** — 스페이스 CRUD/전환, 스페이스별 JSON 영속화(+스키마 버전), 위젯 registry(D-011)와 위젯 6종(todo/memo/timer/clock/kanban/browser), MVP 데이터 변환기(테스트 포함). 검증: CDP로 실제 UI 조작(D-013) → 스페이스 2개·todo 입력·줌 182% → 재시작 → 전부 복원
- 2026-08-16: **자동정렬**(D-014) 그리드 정렬 + 카메라 fit, 단축키 G/F. 검증: 5개 위젯이 2열 그리드로 재배치 + 75% 자동 맞춤
- 2026-08-16: **Phase 3**(D-015) 스페이스별 세션 파티션, 웹뷰 hibernation. 검증: 10%로 줌아웃 시 스냅샷 표시·웹뷰 프로세스 소멸 → 복귀 시 재생성, `Partitions/space-<id>` 생성 확인
- 2026-08-16: **z-order**(스키마 v2) 위젯 클릭 시 앞으로. **배경 선택기**(월페이퍼 3 + 컬러 6)
- 2026-08-16: **Phase 4**(D-017) PiP 미니 모드. 검증: 1440×900 → 380×300 always-on-top → 복귀
- 2026-08-16: **앰비언스**(D-016, 스키마 v3) Web Audio 합성 rain/fire/cafe 믹서. 검증: v2 파일이 로드 시 v3로 마이그레이션되고 슬라이더 값 영속
- 2026-08-16: **셸 침범 수정**(D-018) 캔버스 안전 영역 + 네이티브 뷰 클리핑. **정렬 4종**(D-019). 검증: 캔버스 left=256 인셋, rows/cascade 좌표 확인
- 2026-08-16: **포커스 세션 + Insights**(D-020), **위젯 2종 추가**(calendar, bookmarks). 검증: 세션 3초가 `focus-stats-v1`에 기록·영속, 북마크의 "Open on canvas"가 브라우저 위젯 생성
- 2026-08-16: **정렬 = 열 개수 선택**(D-021), **이미지 저장 기반**(D-022) + Photo·배경 업로드, **Sketch 위젯**(D-023). 위젯 총 10종. 검증: 3열/2열 정렬 좌표, `focusdesk-image://`로 이미지 로드, 스케치 획 3점이 디스크에 영속. `npm test` 36 pass
- 2026-08-16: **웹뷰 클리핑 버그 수정**(D-025) bounds는 전체 크기 유지(리플로우 제거), 셸과 겹치면 `setVisible(false)`+스냅샷. `npm test` 37 pass
- 2026-08-16: **웹뷰 후속 3종**(D-026) 위에 겹친 위젯 있으면 뷰 숨김(=일반 위젯과 같은 스택 동작), 리사이즈 핸들 자리 확보, 줌아웃 시 파괴 대신 숨김(음악 유지)
- 2026-08-16: **새 레포로 이전** `chyon8/focus_desk_main`, **dmg 빌드**(D-027). 검증: `release/Focus Desk-0.1.0-arm64.dmg` 생성, .app 실행 확인
- 2026-08-16: **YouTube Music = 브라우저 프리셋**(D-028) 컨트롤바 버튼 1개. Reader는 보류 결정

## 다음 할 일
1. 배포(D-027): dmg 빌드는 됨. **Developer ID 인증서 확보 후** 공증 + 자동 업데이트 / 앱 아이콘 제작 / `legacy/` 삭제
2. **사용성 QA**(사용자 요청) — UX가 최선인지, 실사용 중 오류 없는지 훑는 라운드
3. Reader 위젯: D-028에서 보류 결정. 원하면 착수 (readability 의존성 필요)
4. 미검증: 60fps 실측, 앰비언스 실제 소리(합성 로직은 테스트됨, 질감 조정은 나중으로 미룸)

## 열린 질문 (사용자 결정 대기)
- 앱 이름 확정 (현재 가칭 Focus Desk)

## 규칙 요약
- git 명령은 사용자가 직접 실행 (CLAUDE.md 워크플로우 준수)
- 문서는 300줄 이하 유지, 이 파일은 50줄 이하 유지
