# STATUS — 세션 스타팅 포인트

> 매 세션 이 파일부터 읽는다. 다른 문서는 필요할 때만 연다.
> 작업이 끝날 때마다 이 파일을 갱신한다 (최근 완료 / 다음 할 일).

## 현재 단계
**Phase 0~5(ROADMAP) 전부 완료. 지금은 실사용 QA 기반 개선 + 앱 서피스(D-038) 진행 중.**

> **다음 세션에서 "착수해"라고 하면 아래 "다음 할 일"을 위에서부터 코딩한다.** 캔버스 밀도(1·2단계)·앱 자동 실행 폐기·브라우저 링크는 2026-08-18에 끝났다. 계획은 각 항목이 가리키는 문서에 원인·단계·검증 기준까지 다 있다. **"결정 필요"라고 적힌 지점은 그 단계에 도달했을 때 사용자에게 물어본다** — 혼자 정하지 말고, 미리 몰아서 묻지도 말 것.

## 문서 맵 (필요할 때만 참조)
| 문서 | 내용 | 언제 읽나 |
|---|---|---|
| [VISION.md](VISION.md) | 컨셉, 타겟, 하지 않을 것, 디자인 방향 | 기능/방향 판단이 필요할 때 |
| [DECISIONS.md](DECISIONS.md) | 스택·기술 결정 + 이유 (최신순, 54개) | 기술 선택하거나 뒤집고 싶을 때 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 폴더 구조, 좌표계, 상태, 브라우저 전략 | 코드 작성 전 |
| [APP-SURFACE.md](APP-SURFACE.md) | 외부 앱을 공간 안에서 — 헬퍼 프로토콜·상태머신·Phase A~E | 앱 위젯 작업 전 (D-038·039) |
| [ROADMAP.md](ROADMAP.md) | ⚠️ 은퇴됨 — Phase 0~5 완료 기록 + 장기 백로그만 남음 | 옛 Phase 이력 궁금할 때만 |

## 최근 완료
- 2026-08-16-17: **Phase 0~5**(MVP·ZUI·위젯 10종·브라우저·앰비언스·포커스 세션, [ROADMAP.md](ROADMAP.md)) + **테마 시스템**(6종, D-033) / 씬 폐기(D-034)→배경 고정(D-035) / 드래그 스트립 구멍(D-036) / **체류 시간**(D-037) / 앱 아이콘·이름 확정 / 유튜브 자동재생·월페이퍼 스캔·빌드 배경 경로 수정
- 2026-08-17: **앱 서피스**(D-038~042, [APP-SURFACE.md](APP-SURFACE.md)) — 배경층 아이디어 폐기 후 Phase A(앱 위젯·시간 판정·앱별 통계)/B(1fps 썸네일)/C(실제 창 배치) 구현. 캔버스 줌인 화질 저하 원인(`will-change` 상시 적용) 수정. macOS Spaces·전체화면 창 배치 실패 원인 규명 후 수정(D-041). LIVE 왕복 수단(⌥Space 전역 단축키 + 위젯 클릭 재부상) 추가(D-042) — 위젯 클릭 왕복 확인, ⌥Space 미검증
- 2026-08-17: **앱 서피스 Phase C·D 마감**(D-044~051) — 종료·창 닫기 시 원래 창 크기 복원(D-044) / 창 여러 개는 제목 기억 + 창 배정 UI(D-045·048) / 공간 진입 시 배경 실행(D-046) / 라이브 썸네일 폐기 → 아이콘 얼굴(**화면기록 권한 제거**, D-047) / 최대화 위젯 줌 역보정(D-049) / 전체화면 중 왕복 복구(D-051 — simple fullscreen이 창 레벨을 올려 앱이 위로 못 옴). **전부 실기 미검증**
- 2026-08-17: **PiP 삭제**(D-052) / 시간 저장본 2배 증식 버그(D-050, StrictMode 이중 load — 오염 값은 로드 시 폐기) / 종료 시 편집 유실 수정(D-053) / DECISIONS 정리(죽은 결정 8개 삭제, 51 → 45개)
- 2026-08-18: **캔버스 밀도** — 상단 크롬 84px 인셋 / `packGrid` 열별 셀 폭 / `FIT_PADDING` 48→20. 사이드바 자동 접기 보류, **자동 열 수 조정은 구현했다가 체감 차이 없어 되돌림**. **F(fit)는 카메라만 움직인다**(위젯을 안 옮김)
- 2026-08-18: **브라우저 링크 해결**(사용자 확인) — `<a target="_blank">`는 팝업 경로를 안 타므로 게스트 페이지에서 클릭을 가로챈다(`browserLinks.ts`, D-055 — 개발자도구 창을 닫아도 배치가 풀리던 버그 같이 수정) / **앱 자동 실행 폐기**(D-054, 아이콘 클릭할 때만 실행) / **펼침(Spread) 보류** — Phase E를 구현했다가 되돌림, 얻은 것은 [APP-SURFACE.md](APP-SURFACE.md) Phase E에

- 2026-08-19: **셸 정리 + 위젯 조작**(D-056~060) — 위젯 추가는 사이드바 팔레트(클릭=가운데, 드래그=놓은 자리) / 하단 컨트롤바 폐기(통계·정리·맞춤·줌%는 사이드바, ⌥G·⌥F 추가) / 브라우저 페이지 줌(⌘+·⌘−·⌘0) / **위젯 리사이즈가 본문을 확대**(폭 기준 0.5~3배, 브라우저 제외) / Esc로 위젯 닫기 / **다중 선택**(⇧드래그 밴드·⌥클릭·⌥hover 테두리 — 선택만 정렬·맞춤·이동) / **Esc는 안 지운다 + 위젯 닫기 Undo 토스트**(D-061). **전부 실기 미검증**

- 2026-08-20: **날씨(파티클)를 공간이 직접 고른다**(D-066 — 테마에서 분리, Clear 포함 5종 + 세기. 월페이퍼 바꿔도 눈이 계속 내리던 문제)
- 2026-08-20: **새 탭 링크 → 새 브라우저 위젯**(D-065, D-055 뒤집음 — 예전엔 보던 페이지를 덮어썼다). **실기 미검증**
- 2026-08-20: **위젯 추가가 커서 자리에서**(D-063 — 빈 캔버스 더블클릭 + N/⌥N 팝오버, 사이드바 팔레트는 유지) / **단축키 표 `?`**(D-064, 앱 안에 처음 생김)
- 2026-08-20: **G가 화면을 채운다**(D-062) — 정리할 때 위젯 크기를 화면에서 역산한 격자 셀에 맞춰 키운다(각자 비율 유지). 열 수는 덮는 넓이가 최대인 것으로 자동 선택. 전체화면·작은 창 모두 동일 계산. **실기 미검증**

## 다음 할 일
1. **캔버스 밀도** — ⓐ("한눈에 보기")는 2026-08-20에 G로 구현(D-062). 남은 건 ⓑ헤더 줌 역보정 ⓒ줌 0.6 미만 LOD — [ARCHITECTURE.md](ARCHITECTURE.md) "캔버스 밀도·가독성"
2. **공간 삭제 보호**(미착수) — 위젯은 Undo 토스트로 끝났지만(D-061), 사이드바 🗑는 JSON 파일+누적 시간까지 **즉시 영구 삭제**다([Sidebar.tsx](../src/app/Sidebar.tsx)). 여기는 되돌리기가 아니라 확인 다이얼로그(공간 이름 확인)가 맞다
3. **로컬 빌드로 실사용**(D-044~055) — `npm run dist`로 빌드해 며칠 쓰면서 창 배정·시간 값·전체화면 왕복 실기 확인. **패키징본은 접근성 권한을 새로 줘야 함**(바이너리 경로 기준)
4. **사용성 QA** — UX가 최선인지, 실사용 오류 없는지 훑는 라운드 (2번 검증 후)
5. **배포 마무리**(D-027) — Developer ID 인증서 → 공증 + 자동 업데이트. 헬퍼 바이너리도 서명 대상
6. 미검증: ⌥Space 왕복(위젯 클릭 왕복은 확인됨), 60fps 실측, 앰비언스 소리 품질 / 정리: `legacy/` 삭제 / Reader 위젯 보류(D-028)

## 정리 대기 (급하지 않음, 발견 시 같이 처리)
- dev 실행 시 `unset ELECTRON_RUN_AS_NODE` 필요(VSCode 확장 환경이 넣는 변수, 그대로면 Electron이 node 모드로 떠서 죽음). 사용자 터미널에선 무관
- **유튜브 진짜 전체화면**(순위 내림, 2026-08-17). `webview`의 `enter-html-full-screen`/`leave-html-full-screen`을 받아 위젯 최대화 + 앱 전체화면을 자동으로 같이 걸고 같이 풀기(버튼 추가 없음, `browserFullscreen.ts` 같이 정리)
- 체류 시간: 디스플레이만 꺼진 상태(시스템 슬립·잠금 아님)는 계속 카운트됨. 유휴 감지는 의도적으로 안 넣음(D-037)
- **웹 로그인이 한 번 풀렸다가 돌아옴**(2026-08-18, 사용자 확인). 앱 코드엔 세션·쿠키를 지우는 곳이 없고 파티션·쿠키 DB도 그대로였다. 원인 미확정, 재현되면 그때 판다 — 유력 후보는 쿠키 값 암호화 키(macOS 키체인 "Safe Storage")
- 빌드본에서는 월페이퍼가 asar 안이라 폴더에 넣어도 안 보임 — 필요하면 userData 폴더도 같이 스캔
- 위젯 데이터의 `theme: 'LIGHT'|'DARK'` 필드가 미사용 — 지우려면 스키마 마이그레이션 필요
- `src/spaces/backgrounds.ts`의 `isLightBackground()`는 dead code (테마의 `mood`가 대체)

## 열린 질문 (사용자 결정 대기)
- **월페이퍼 조달**: 라이선스 명확한 곳(itch.io, Unsplash/Pexels)에서 사용자가 구해온다. 장수보다 **톤 통일**(같은 작가/시리즈 3~4장). 현재 8장
- **디자인 톤**(합의됨): 감성은 배경·빛·사운드·여백에서, 위젯 크롬은 조용하게

## 규칙 요약
- git 명령은 사용자가 직접 실행 (CLAUDE.md 워크플로우 준수)
- 문서는 300줄 이하 유지, 이 파일은 50줄 이하 유지 (늘어나면 "최근 완료"부터 압축)
