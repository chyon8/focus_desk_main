# STATUS — 세션 스타팅 포인트

> 매 세션 이 파일부터 읽는다. 다른 문서는 필요할 때만 연다.
> 작업이 끝날 때마다 이 파일을 갱신한다 (최근 완료 / 다음 할 일).

## 현재 단계
**Phase 0~5(ROADMAP) 전부 완료. 지금은 실사용 QA 기반 개선 + 앱 서피스(D-038) 진행 중.**

> **다음 세션에서 "착수해"라고 하면 아래 "다음 할 일"을 위에서부터 코딩한다.** 캔버스 밀도(1·2단계)·앱 자동 실행 폐기·브라우저 링크는 2026-08-18에 끝났다. 계획은 각 항목이 가리키는 문서에 원인·단계·검증 기준까지 다 있다. **"결정 필요"라고 적힌 지점은 그 단계에 도달했을 때 사용자에게 물어본다** — 혼자 정하지 말고, 미리 몰아서 묻지도 말 것.

## 문서 맵 (필요할 때만 참조)
| 문서 | 내용 | 언제 읽나 |
|---|---|---|
| [VISION.md](VISION.md) | 컨셉, 타겟, 하지 않을 것, 디자인 방향 | 기능/방향 판단이 필요할 때 |
| [DECISIONS.md](DECISIONS.md) | 스택·기술 결정 + 이유 (최신순, 55개) | 기술 선택하거나 뒤집고 싶을 때 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 폴더 구조, 좌표계, 상태, 브라우저 전략 | 코드 작성 전 |
| [APP-SURFACE.md](APP-SURFACE.md) | 외부 앱을 공간 안에서 — 헬퍼 프로토콜·상태머신·Phase A~D | 앱 위젯 작업 전 (D-038·039) |
| [ROADMAP.md](ROADMAP.md) | ⚠️ 은퇴됨 — Phase 0~5 완료 기록 + 장기 백로그만 남음 | 옛 Phase 이력 궁금할 때만 |

## 최근 완료
- 2026-08-16~17: **Phase 0~5**(MVP·ZUI·위젯 10종·브라우저·앰비언스·포커스 세션, [ROADMAP.md](ROADMAP.md)) + **테마 6종**(D-033) / 씬 폐기→배경 고정(D-034·035) / **체류 시간**(D-037) / **PiP 삭제**(D-052) / 시간 2배 증식(D-050)·종료 시 편집 유실(D-053) 수정
- 2026-08-17: **앱 서피스 A~D**(D-038~051) — 앱 위젯·시간 판정·앱별 통계 / 실제 창 배치 / 원래 크기 복원(D-044) / 창 배정 UI(D-045·048) / 썸네일 폐기 → **화면기록 권한 제거**(D-047) / macOS Spaces·전체화면 대응(D-041·051)
- 2026-08-18: **캔버스 밀도**(상단 크롬 인셋·열별 셀 폭·F는 카메라만) / **브라우저 링크**(게스트에서 클릭 가로채기, D-055) / **앱 자동 실행 폐기**(D-054)
- 2026-08-19: **셸 정리 + 위젯 조작**(D-056~061) — 사이드바 팔레트 / 하단 바 폐기 / 페이지 줌 / 리사이즈가 본문 확대 / 다중 선택 / Esc는 안 지움 + 닫기 Undo
- 2026-08-20: **G가 화면을 채운다**(D-062) / **위젯 추가가 커서 자리에서**(D-063) / **단축키 표 `?`**(D-064) / **새 탭 링크 → 새 위젯**(D-065) / **날씨를 공간이 고른다**(D-066)
- 2026-08-22: **앱 창을 위젯 자리에서 그대로 쓴다**(D-067, [APP-SURFACE.md](APP-SURFACE.md)) — 최대화 전용 폐기, 창 여러 개 동시. 위젯이 캔버스에 온전히 보일 때만 창이 붙음(팬=위치만, 줌=멈춘 뒤 크기) / **앱이 열린 동안 데스크는 창 레벨 −1** → "click to return"이 뜰 일 자체를 없앰, ⌥Space는 두 겹 토글 / **창 모드에서 앱이 앞으로 못 나오던 버그 수정**(`setVisibleOnAllWorkspaces`가 창 레벨을 올림) / Electron 앱이 리사이즈를 삼키면 90ms 뒤 재적용 / **창을 직접 옮기면 위젯이 따라오고, 캔버스를 벗어나면 슬롯을 놓아준다**

## 다음 할 일
1. **D-067 실기 검증**(최우선, 아직 아무것도 확인 안 됨) — ⓐ앱 2개 동시에 각자 위젯 자리 ⓑ데스크 빈 곳 클릭해도 앱이 안 뒤로 감(레벨 −1) ⓒ⌥Space 두 겹 토글 ⓓ창 가장자리를 끌면 위젯이 따라옴 ⓔMagnet 전체화면 → "out of its slot" → 다시 데려오기 ⓕ위젯을 사이드바 쪽으로 밀면 창이 원래 크기로 빠짐
2. **앱 창이 위젯보다 클 때**(미착수) — 위젯이 작거나 앱이 리사이즈를 거부하면(FL Studio) 창이 위젯을 넘어 **이웃을 덮는다**. 예전 계획의 600×400 컷은 안 넣었다. 고칠 방법은 하나: `place` 응답의 실제 rect가 요청과 다르면 **위젯을 그 크기에 맞춘다**(이미 `onWindowFrame`이 하는 일을 첫 배치에도)
3. **공간 삭제 보호**(미착수) — 사이드바 🗑는 JSON 파일+누적 시간까지 **즉시 영구 삭제**다([Sidebar.tsx](../src/app/Sidebar.tsx)). 확인 다이얼로그(공간 이름 입력)가 맞다
4. **캔버스 밀도** 나머지 — ⓑ헤더 줌 역보정 ⓒ줌 0.6 미만 LOD ([ARCHITECTURE.md](ARCHITECTURE.md) "캔버스 밀도·가독성")
5. **로컬 빌드로 실사용** → **사용성 QA** — `npm run dist`로 며칠 쓰기. **패키징본은 접근성 권한을 새로 줘야 함**(바이너리 경로 기준)
6. **배포 마무리**(D-027) — Developer ID → 공증 + 자동 업데이트. 헬퍼 바이너리도 서명 대상
7. 백로그: 같은 앱 창 2개 동시(헬퍼 키를 `appKey+title`로) / 60fps 실측 / 앰비언스 소리 품질 / `legacy/` 삭제 / Reader 위젯(D-028)

## 정리 대기 (급하지 않음, 발견 시 같이 처리)
- dev 실행 시 `unset ELECTRON_RUN_AS_NODE` 필요(VSCode 확장 환경이 넣는 변수, 그대로면 Electron이 node 모드로 떠서 죽음). 사용자 터미널에선 무관
- **유튜브 진짜 전체화면**(순위 내림, 2026-08-17). `webview`의 `enter-html-full-screen`/`leave-html-full-screen`을 받아 위젯 최대화 + 앱 전체화면을 자동으로 같이 걸고 같이 풀기(버튼 추가 없음, `browserFullscreen.ts` 같이 정리)
- 체류 시간: 디스플레이만 꺼진 상태(시스템 슬립·잠금 아님)는 계속 카운트됨. 유휴 감지는 의도적으로 안 넣음(D-037)
- **데스크 창 레벨 −1의 부작용**(D-067): 앱이 열려 있는 동안 Finder·Slack 같은 **관계없는 창 밑으로도** 들어간다. ⌥Space로 올릴 수 있지만, 실사용에서 거슬리면 "앱 창들보다만 아래"로 좁힐 방법을 찾아야 한다
- 헬퍼가 놓인 창 프레임을 **0.3초마다 폴링**한다(D-067). 열린 창이 많을 때 비용은 실측 안 됨
- **웹 로그인이 한 번 풀렸다가 돌아옴**(2026-08-18, 사용자 확인). 앱 코드엔 세션·쿠키를 지우는 곳이 없고 파티션·쿠키 DB도 그대로였다. 원인 미확정, 재현되면 그때 판다 — 유력 후보는 쿠키 값 암호화 키(macOS 키체인 "Safe Storage")
- 빌드본에서는 월페이퍼가 asar 안이라 폴더에 넣어도 안 보임 — 필요하면 userData 폴더도 같이 스캔
- 위젯 데이터의 `theme: 'LIGHT'|'DARK'` 필드가 미사용 — 지우려면 스키마 마이그레이션 필요
- `src/spaces/backgrounds.ts`의 `isLightBackground()`는 dead code (테마의 `mood`가 대체)

## 열린 질문 (사용자 결정 대기)
- **월페이퍼 조달**: 라이선스 명확한 곳(itch.io, Unsplash/Pexels)에서 사용자가 구해온다. 장수보다 **톤 통일**(같은 작가/시리즈 3~4장). 현재 8장
- **디자인 톤**(합의됨): 감성은 배경·빛·사운드·여백에서, 위젯 크롬은 조용하게

## 규칙 요약
- git 명령은 사용자가 직접 실행 (CLAUDE.md 워크플로우 준수)
- 문서는 300줄 이하 유지, 이 파일은 50줄 이하 유지 (늘어나면 "최근 완료"부터 압축)
