# STATUS — 세션 스타팅 포인트

> 매 세션 이 파일부터 읽는다. 다른 문서는 필요할 때만 연다.
> 작업이 끝날 때마다 이 파일을 갱신한다 (최근 완료 / 다음 할 일).

## 현재 단계
**Phase 0~5(ROADMAP) 전부 완료. 지금은 실사용 QA 기반 개선 단계 — Phase 개념 없이 이 파일의 "다음 할 일"로만 진행.**

> **다음 세션에서 "다음꺼 해"라고 하면 아래 1번을 바로 착수한다. 추가 확인 불필요 — 범위·방향 모두 합의 끝(2026-08-17).**

## 문서 맵 (필요할 때만 참조)
| 문서 | 내용 | 언제 읽나 |
|---|---|---|
| [VISION.md](VISION.md) | 컨셉, 타겟, 하지 않을 것, 디자인 방향 | 기능/방향 판단이 필요할 때 |
| [DECISIONS.md](DECISIONS.md) | 스택·기술 결정 + 이유 (append-only) | 기술 선택하거나 뒤집고 싶을 때 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 폴더 구조, 좌표계, 상태, 브라우저 전략 | 코드 작성 전 |
| [ROADMAP.md](ROADMAP.md) | ⚠️ 은퇴됨 — Phase 0~5 완료 기록 + 장기 백로그만 남음 | 옛 Phase 이력 궁금할 때만 |

## 최근 완료
- 2026-08-16: **Phase 0~5** (MVP 이식, ZUI 코어, 스페이스/위젯 10종, 브라우저, PiP, 앰비언스, 포커스 세션) — 상세는 [ROADMAP.md](ROADMAP.md)와 DECISIONS D-001~D-024
- 2026-08-16: 브라우저 위젯 `<webview>` 전면 교체(D-029), 새 레포 이전 + dmg 빌드(D-027·028), 앱 전체화면·위젯 최대화(D-030·031), 상단 셸 클릭 먹힘 수정(D-032)
- 2026-08-16: **테마 시스템** — 테마 6종(씬+파티클+색/폰트 토큰), ThemePicker, 유튜브 SPA 이동 시 URL 저장 버그 수정
- 2026-08-16: **전면 토큰화**(D-033) — 하드코딩 색 0개, **위젯 본체 투명화**(배경이 비침), 위젯별 LIGHT/DARK 토글 제거
- 2026-08-17: **코드로 그리는 씬 폐기**(D-034) — CSS/SVG·three.js 둘 다 만들었으나 원하는 느낌이 아니라 삭제, `three` 의존성 제거
- 2026-08-17: **배경 고정**(D-035) — 줌할 때 배경이 미끄러지던 패럴랙스·드리프트 제거
- 2026-08-17: 유튜브 자동재생 차단(재시작 시), 월페이퍼 폴더 런타임 스캔(picker 스크롤), 빌드에서 배경 경로 깨지던 것 수정
- 2026-08-17: **드래그 스트립 구멍 뚫기**(D-036) — 최상단에 붙인 위젯이 드래그되던 문제
- 2026-08-17: **스페이스별 체류 시간**(D-037) — 창 포커스 중에만, 날짜별로 적립·저장. 사이드바에 오늘 시간, Insights는 이 데이터로 교체

## 다음 할 일
1. **PiP 삭제 + "몰입" 버튼** ← 여기부터. 사용자 결정 완료(2026-08-17): PiP는 **쓰지 않으므로 제거**한다
   - 제거 대상: `MiniView.tsx`, `uiStore`의 mini 상태(`miniWidgetId`/`enterMini`/`exitMini`), `WidgetFrame`의 ⧉ 버튼, `App.tsx`의 isMini 분기, `electron/ipc/window-mode.ts`의 set-mini, `preload.ts`·`vite-env.d.ts`의 해당 타입, `index.css`의 `.mini-shell`
   - `--surface` 토큰은 Photo/Sketch(`.photo-paper`)와 모달 스크림도 쓰므로 **남긴다**
   - 이어서 **몰입 버튼**: 위젯 헤더에 버튼 1개 → 앱 전체화면 + 위젯 최대화 동시 적용, Esc 한 번에 동시 해제. 지금은 3번 눌러야 함(영상 전체화면 / ⛶ 최대화 / ⌃⌘F). 관련: `WidgetFrame.tsx`, `uiStore`, `useKeyboardShortcuts.ts`
   - 끝나면 DECISIONS에 D-036으로 기록(D-017 뒤집음)
2. **사용성 QA** — UX가 최선인지, 실사용 오류 없는지 훑는 라운드
3. **바탕화면 레이어 모드**(조사 필요) — 데스크탑 배경층에 고정, 다른 앱이 위로. macOS는 NSWindow level을 `kCGDesktopWindowLevel`로 내려야 하는데 Electron API에 없음 → 네이티브 애드온/Übersicht 방식 조사
4. **배포 마무리**(D-027) — Developer ID 인증서 → 공증 + 자동 업데이트, 앱 아이콘
5. 미검증: 60fps 실측, 앰비언스 소리 품질 / 정리: `legacy/` 삭제
6. Reader 위젯: 보류(D-028). 원하면 착수

## 정리 대기 (급하지 않음, 발견 시 같이 처리)
- 체류 시간: 디스플레이만 꺼진 상태(시스템 슬립·잠금 아님)는 계속 카운트됨. 유휴 감지는 의도적으로 안 넣음(D-037)
- 빌드본에서는 월페이퍼가 asar 안이라 폴더에 넣어도 안 보임 — 필요하면 userData 폴더도 같이 스캔
- 위젯 데이터의 `theme: 'LIGHT'|'DARK'` 필드가 미사용 — 지우려면 스키마 마이그레이션 필요
- `src/spaces/backgrounds.ts`의 `isLightBackground()`는 dead code (테마의 `mood`가 대체)

## 열린 질문 (사용자 결정 대기)
- 앱 이름 확정 (현재 가칭 Focus Desk)
- **월페이퍼 조달**: 코드로 그리는 씬은 폐기(D-034). 사진·일러스트는 **사용자가 라이선스 명확한 곳에서 구해온다** — itch.io(픽셀아트·일러스트, 라이선스 명시), Unsplash/Pexels(사진, 상업 무료). 장수보다 **톤 통일**이 중요(같은 작가/시리즈 3~4장). 현재 사진 테마는 3장뿐이라 빈약함
- **디자인 톤**: cramandconquer 레퍼런스는 `ref_images/`(gitignore됨)에서 확인 완료. 합의된 방향 — 감성은 배경·빛·사운드·여백에서, 위젯 크롬은 조용하게

## 규칙 요약
- git 명령은 사용자가 직접 실행 (CLAUDE.md 워크플로우 준수)
- 문서는 300줄 이하 유지, 이 파일은 50줄 이하 유지 (늘어나면 "최근 완료"부터 압축)
